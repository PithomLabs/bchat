1) In Settings > Admin > Member > when you click the vertical dots button to Edit User, the modal should display all field values if they have been populated already from the database including the password so that if I change the Company or any field, I can update one or only the fields I need to update

2) when I changed any field in Edit User (same modal as above), ensure that the fields have been saved to database accordingly

3) In the home page login as ate, I already made the company update but the error says: user is not associated with any company (verify that and fix accordingly)

deep dive into the issues above and write plan on how to fix the underlying problem (not band-aid fix), also read the codebase if there is anything related to changes that we are about to make that may have corresponding effects on other parts of the codebase, ensure that you surgically fix the needed parts only