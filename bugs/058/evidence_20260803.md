# Bug 058 Evidence — Live CockroachDB Execution Output

**Date:** 2026-08-03  
**Target Cluster:** CockroachDB Cloud Basic (Serverless, v26.2.1)  
**DSN:** `postgresql://bchat:***@great-goat-30894.j77.cockroachlabs.cloud:26257/bchat?sslmode=verify-full`

---

## Task A — Cluster Setting Probe (Live Execution)

### Statement 1
```sql
SET CLUSTER SETTING feature.vector_index.enabled = true;
```
**Literal Terminal Output:**
```
SET CLUSTER SETTING
Time: 677ms
```
**Status:** ✅ SUCCEEDED

---

### Statement 2
```sql
SET serial_normalization = 'sql_sequence';
```
**Literal Terminal Output:**
```
SET
Time: 50ms
```
**Status:** ✅ SUCCEEDED

---

### Statement 3
```sql
SET CLUSTER SETTING kv.range_merge.queue_interval = '50ms';
```
**Literal Terminal Output:**
```
ERROR: unknown cluster setting 'kv.range_merge.queue_interval'
SQLSTATE: 42P02
Failed running "sql"
```
**Status:** ❌ REJECTED (SQLSTATE 42P02)

---

### Statement 4
```sql
SET CLUSTER SETTING jobs.registry.interval.gc = '30s';
```
**Literal Terminal Output:**
```
SET CLUSTER SETTING
Time: 643ms
```
**Status:** ✅ SUCCEEDED (Unexpected success on Basic tier)

---

### Statement 5
```sql
SET CLUSTER SETTING jobs.retention_time = '15s';
```
**Literal Terminal Output:**
```
SET CLUSTER SETTING
Time: 703ms
```
**Status:** ✅ SUCCEEDED (Unexpected success on Basic tier)

---

### Statement 6
```sql
SET CLUSTER SETTING sql.stats.automatic_collection.enabled = false;
```
**Literal Terminal Output:**
```
SET CLUSTER SETTING
Time: 605ms
```
**Status:** ✅ SUCCEEDED (Unexpected success on Basic tier)

---

## Task A.1 — Vector Index `IF NOT EXISTS` Syntax Check

```sql
SET sql_safe_updates = off;
CREATE VECTOR INDEX IF NOT EXISTS idx_probe_test ON agent_vectors (embedding);
```
**Literal Terminal Output:**
```
SET
CREATE VECTOR INDEX
Time: 36.657s
```
**Cleanup Statement:**
```sql
DROP INDEX IF EXISTS idx_probe_test;
```
**Literal Terminal Output:**
```
DROP INDEX
Time: 3.173s
```
**Status:** ✅ SUCCEEDED (`CREATE VECTOR INDEX IF NOT EXISTS` parses and executes natively in v26.2.1)

---

## Summary of Findings

1. **Setting Probe Divergence**:
   - `feature.vector_index.enabled` & `serial_normalization`: ✅ SUCCEEDED (as expected).
   - `kv.range_merge.queue_interval`: ❌ REJECTED (`SQLSTATE 42P02: unknown cluster setting`).
   - `jobs.registry.interval.gc`, `jobs.retention_time`, & `sql.stats.automatic_collection.enabled`: ✅ **SUCCEEDED** on the live CockroachDB Cloud Basic tier cluster. (This differs from the hypothesis that all 4 internal settings would be rejected).
2. **Vector Index Syntax**:
   - `CREATE VECTOR INDEX IF NOT EXISTS` parses and executes cleanly in CockroachDB v26.2.1. The `SQLSTATE 42P07` fallback trap in `vectordb_cockroach.go` is a safe workaround, but `IF NOT EXISTS` is supported directly by the DDL parser.
