for tickets under the ticket modal/form regardless if it is new ticket or edit ticket the visibility of the ticket or memo must be set to a new setting like public because we are retrofitting the memos app as a ticket management system but only visible to internal users (the admin and internal support system) and the external user (the customer), is there such a public-like setting that meets these requirements? you can look at ticket #11 for context details

due to lack of this proper setting, when you click on the memo URL (description) in the ticket modal, it results to 403 error

let us make this interactive question and answer until we agree to a plan, ask me anything, deep dive into the root cause and write plan to fix the underlying problem (no band-aid fix)