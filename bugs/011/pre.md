## prompt

as internal support or admin under Edit Ticket modal, when you add comment, it should 1) automatically add that to the associated memo description and 2) it should add that same comment below the Comments section of the Edit Ticket modal and 3) should update the comment counter initially indicated as Comments (0) 

let us make this interactive question and answer until we agree to a plan

deep dive into the root cause and write plan to fix the underlying problem (no band-aid fix)

do not modify code or docs until I give the approval


## prompt

also when I clicked Add Comment, which setting should we choose for the comment to work in this ticket management system? the goal is to make this visible so we can remove that dropdown option so users (regardless if internal or external users) don't need to think about it, add this to your implementation plan, ensure that you read the codebase and its implications in relation to other entities that may be affected by this change