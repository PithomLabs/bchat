package agent

import (
	"testing"
)

func TestProcessPendingTickets(t *testing.T) {
	t.Skip("Requires real CockroachDB + store — see integration test suite")
}

func TestEmbedTenantTickets(t *testing.T) {
	t.Skip("Requires real CockroachDB + store — see integration test suite")
}

func TestBuildTicketClusters(t *testing.T) {
	t.Skip("Requires real CockroachDB + store — see integration test suite")
}

func TestEscalateTicket(t *testing.T) {
	t.Skip("Requires real CockroachDB + store — see integration test suite")
}
