# Plan: Review test_plan.md against codebase

## Status
**Review mode only** — no code/docs changes. Switch to code mode to execute.

## Findings

### 1. TestGetCurrentSchemaVersion — Approve with Nits
- **Root cause is correct**: test hardcodes "0.24.2"; actual is "0.25.29".
- **Nit 1**: The proposed fix hardcodes `0.25.29`. This will break on every new migration. Prefer a minor-version check (e.g. `require.Contains(t, currentSchemaVersion, version.GetMinorVersion(version.DevVersion))`) so it stays green across patch bumps.
- **Nit 2**: The test number was set by `store/migration/sqlite/0.25/00`–`28` (29 files). The formula `patch+1` yields 29, which matches. But static pairing of that formula to the highest filename couples the test to implementation detail.

### 2. TestTicketForeignKeyConstraints — Approve with Nits
- **Root cause is correct**: `LATEST.sql` lacks FKs on `tickets` while migration `04` adds them via recreate; the test setup uses `LATEST.sql` directly.
- **Nit 1 (scope expansion)**: Proposed `LATEST.sql` change also alters `parent_id` from `INTEGER REFERENCES tickets(id)` to use-style `ON DELETE CASCADE`. That changes cascade semantics versus the existing row but matches migration 04. Calling that out explicitly in the plan would help reviewer confidence.
- **Nit 2 (silent divergence)**: `beads_id` (`TEXT UNIQUE`) and its index definition already differ between migration 04 and `LATEST.sql`. The plan correctly avoids touching this, but it is worth acknowledging that `LATEST.sql` is already not fully consistent with the migration on that column/index.
- **Nit 3**: New index `idx_tickets_assignee_id` is spot-on; migration 04 creates it and LATEST.sql was missing it.

### 3. Pre-existing Parallel Failure Not in Plan
- Store tests also emit: `Column already exists, skipping table=tickets column=type` and `Column already exists, skipping table=tickets column=tags` (mirrored ~10× from `EnsureTicketTypeColumn`). The plan does not address whether these warnings are benign or indicate a schema/history drift worth cleaning up before this fix. Worth confirming in code mode before validating.

## Recommendation
- **Approve with nits**. Both fixes are on the right track. Before executing:
  1. Use the minor-version comparison for `TestGetCurrentSchemaVersion` so the test survives future migrations.
  2. Proceed with `LATEST.sql` FK fix; the explicit mention of the `parent_id` cascade definition change is recommended.
  3. Address or neutralize the `EnsureTicketTypeColumn` repeating warnings if they are actionable.
